<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Compose supplies the real styling; this base theme only needs to keep the
      platform decor out of the way. Parenting on DeviceDefault avoids pulling
      in the Material Components library for a single style.
    -->
    <style name="Theme.Wristbridge" parent="android:Theme.DeviceDefault.DayNight">
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
